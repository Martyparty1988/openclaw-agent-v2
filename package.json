{
  "name": "openclaw-agent",
  "version": "2.2.0",
  "description": "Self-improving autonomous AI agent — WhatsApp + Telegram + Web + Email + Supabase Memory",
  "main": "router.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node scripts/ensure-git-workdir.js && node router.js",
    "dev": "node --watch router.js",
    "web": "npx serve web -p 3000",
    "check": "node --check router.js && node --check meta-agent.js && node --check sub-agents/email.js && node --check sub-agents/learner.js && node --check sub-agents/memory.js && node --check sub-agents/model-presets.js && node --check sub-agents/git-workspace.js && node --check sub-agents/auto-worker.js && node --check scripts/ensure-git-workdir.js",
    "deploy:check": "node scripts/deploy-preflight.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "@builderbot/bot": "^1.2.0",
    "@builderbot/provider-baileys": "^1.2.0",
    "@supabase/supabase-js": "^2.49.4",
    "dotenv": "^16.0.0",
    "node-telegram-bot-api": "^0.66.0",
    "nodemailer": "^6.9.16"
  }
}
